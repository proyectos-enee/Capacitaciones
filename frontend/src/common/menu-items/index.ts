import { MenuItems } from './models/menu-items.ts';
import { AppMenu } from '../../app/menu.ts';

// ==============================|| MENU ITEMS ||============================== //

const menuItems: MenuItems = {
  items: [...AppMenu],
};

export default menuItems;
